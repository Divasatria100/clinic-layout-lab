// Navigation domain constants (Phase 3 refinement).
//
// MAX_SEGMENT_LENGTH: world-space units. Initial path geometry is split
// into evenly distributed segments no longer than this. Deterministic MVP
// default; no user-facing configuration (spec §5).
export const MAX_SEGMENT_LENGTH = 100
